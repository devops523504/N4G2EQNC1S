// Copy this file to js/config.js (which is gitignored) and fill in your
// own Mapbox access token. Get a free token at:
// https://account.mapbox.com/access-tokens/
//
// Do not commit real tokens to a public repository. In the Mapbox
// dashboard, add this site's deployed domain to the token's URL
// restrictions so it can't be reused elsewhere if it ever leaks.
const MAPBOX_ACCESS_TOKEN = "REPLACE_WITH_YOUR_MAPBOX_TOKEN";
